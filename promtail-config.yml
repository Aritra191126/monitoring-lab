server:
  http_listen_port: 9080

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://172.17.0.1:3100/loki/api/v1/push

scrape_configs:
  - job_name: monitoring-lab
    static_configs:
      - targets:
          - localhost
        labels:
          job: monitoring-lab
          __path__: /var/log/app/app.log